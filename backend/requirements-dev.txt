# Development-only extras, on top of requirements.txt:
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# scripts/seed_demo_data.py synthesises sample alert snapshots with OpenCV, so
# it needs cv2/numpy. The server itself does not — keeping these out of
# requirements.txt is what keeps the deployed image small.
opencv-python-headless==4.9.0.80
numpy==2.4.6

# Test suite.
pytest==8.2.0
